<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:Viewmodel="clr-namespace:UniversalRobotsApp.ViewModel"
             x:Class="UniversalRobotsApp.Pages.Robots"
             Title="Robots">
	
	<ContentPage.BindingContext>
		<Viewmodel:RobotsViewModel/>
	</ContentPage.BindingContext>
	
	<ListView ItemsSource="{Binding RobotsList}"
			  x:Name="RobotsListView"
			  ItemSelected="OnItemSelected">
		<ListView.ItemTemplate>
			<DataTemplate>
				<ViewCell>
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="*"/>
							<RowDefinition Height="*"/>
						</Grid.RowDefinitions>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="2*"/>
						</Grid.ColumnDefinitions>
						<Image Source="{Binding Image}" Grid.Column="0" Grid.RowSpan="2" Aspect="AspectFit"/>
						<Label Text="{Binding Id}" Grid.Column="1" Grid.Row="0"/>
						<Label Text="{Binding Name}" Grid.Column="1" Grid.Row="1"/>
					</Grid>
				</ViewCell>
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</ContentPage>